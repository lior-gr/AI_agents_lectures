# Video Story: learning-process-fast.mp4

## Narrative Intent
Show the real lesson flow across tutorial pages with emphasis on the P1 "By the end of this page you should be able to..." block, full Lesson 0 vocabulary coverage, smooth-scroll prompt-to-task walkthroughs with copy clicks, centered checkpoint spotlights, and centered page-review sections.

## Story Beats
1. Open `lesson-0-foundations.html` and wait 1.0s.
2. Smooth-scroll to center the white card containing `#p1-outcomes` (optional), then spotlight the entire card and hold for 1.6s.
3. Smooth-scroll to center the white card containing `#p2-vocabulary` (optional), then spotlight the entire card and hold for 1.2s.
4. For each `section.card:has(#p2-vocabulary) .note` (optional), smooth-scroll to center it, spotlight it, then hold for 1.4s.
5. Smooth-scroll to center `section.card:has(#p2-vocabulary) .diagram` (optional), then spotlight and hold for 1.6s.
6. Smooth-scroll to center `section.card:has(#p2-vocabulary) .example` (optional), then spotlight and hold for 1.4s.
7. Smooth-scroll to center the white card containing `#p8-summary` (optional), then spotlight the entire card and hold for 1.6s.
8. Smooth-scroll to center `.checkpoint` (optional), then spotlight and hold for 1.6s.
9. Idle for 0.6s.
10. Click `aside.sidebar a[href='lesson-1-baseline.html']`, then wait 1.1s.
11. Smooth-scroll to center the white card containing `#p1-outcomes` (optional), then spotlight the entire card and hold for 1.6s.
12. For each `.prompt-box` block: smooth-scroll to center and spotlight prompt (~2.0s per 5 lines), then smoothly move mouse to `.copy-btn`, click, hold for 0.5s, and only then move to paired `.task-box` and spotlight for 2.0s.
13. Smooth-scroll to center `.checkpoint` (optional), then spotlight and hold for 1.6s.
14. Idle for 0.6s.
15. Click `aside.sidebar a[href='lesson-2-agent-loop.html']`, then wait 1.1s.
16. Smooth-scroll to center the white card containing `#p1-outcomes` (optional), then spotlight the entire card and hold for 1.6s.
17. For each `.prompt-box` block: smooth-scroll to center and spotlight prompt (~2.0s per 5 lines), then smoothly move mouse to `.copy-btn`, click, hold for 0.5s, and only then move to paired `.task-box` and spotlight for 2.0s.
18. Smooth-scroll to center `.checkpoint` (optional), then spotlight and hold for 1.6s.
19. Idle for 0.6s.
20. Click `aside.sidebar a[href='lesson-3-mcp-decoupling.html']`, then wait 1.1s.
21. Smooth-scroll to center the white card containing `#p1-outcomes` (optional), then spotlight the entire card and hold for 1.6s.
22. For each `.prompt-box` block: smooth-scroll to center and spotlight prompt (~2.0s per 5 lines), then smoothly move mouse to `.copy-btn`, click, hold for 0.5s, and only then move to paired `.task-box` and spotlight for 2.0s.
23. Smooth-scroll to center the white card containing `#p10-scope` (optional), then spotlight the entire card and hold for 1.5s.
24. Smooth-scroll to center `.checkpoint` (optional), then spotlight and hold for 1.6s.
25. Idle for 0.6s.
26. Click `aside.sidebar a[href='lesson-4-skills.html']`, then wait 1.1s.
27. Smooth-scroll to center the white card containing `#p1-outcomes` (optional), then spotlight the entire card and hold for 1.6s.
28. For each `.prompt-box` block: smooth-scroll to center and spotlight prompt (~2.0s per 5 lines), then smoothly move mouse to `.copy-btn`, click, hold for 0.5s, and only then move to paired `.task-box` and spotlight for 2.0s.
29. Smooth-scroll to center the white card containing `#p10-scope` (optional), then spotlight the entire card and hold for 1.5s.
30. Smooth-scroll to center `.checkpoint` (optional), then spotlight and hold for 1.6s.
31. Idle for 0.6s.
32. Click `aside.sidebar a[href='lesson-5-ui-boundary.html']`, then wait 1.1s.
33. Smooth-scroll to center the white card containing `#p1-outcomes` (optional), then spotlight the entire card and hold for 1.6s.
34. For each `.prompt-box` block: smooth-scroll to center and spotlight prompt (~2.0s per 5 lines), then smoothly move mouse to `.copy-btn`, click, hold for 0.5s, and only then move to paired `.task-box` and spotlight for 2.0s.
35. Smooth-scroll to center the white card containing `#p10-teaches` (optional), then spotlight the entire card and hold for 1.5s.
36. Smooth-scroll to center `.checkpoint` (optional), then spotlight and hold for 1.6s.
37. Idle for 0.6s.
38. Click `aside.sidebar a[href='appendix-a-multiple-skills.html']`, then wait 1.1s.
39. Smooth-scroll to center the white card containing `#p1-outcomes` (optional), then spotlight the entire card and hold for 1.6s.
40. For each `.prompt-box` block: smooth-scroll to center and spotlight prompt (~2.0s per 5 lines), then smoothly move mouse to `.copy-btn`, click, hold for 0.5s, and only then move to paired `.task-box` and spotlight for 2.0s.
41. Smooth-scroll to center `.checkpoint` (optional), then spotlight and hold for 1.6s.
42. Idle for 0.6s.
43. Click `aside.sidebar a[href='appendix-b-bounded-routing.html']`, then wait 1.1s.
44. Smooth-scroll to center the white card containing `#p1-outcomes` (optional), then spotlight the entire card and hold for 1.6s.
45. For each `.prompt-box` block: smooth-scroll to center and spotlight prompt (~2.0s per 5 lines), then smoothly move mouse to `.copy-btn`, click, hold for 0.5s, and only then move to paired `.task-box` and spotlight for 2.0s.
46. Smooth-scroll to center `.checkpoint` (optional), then spotlight and hold for 1.6s.
47. Idle for 0.6s.
48. Idle for 1.2s.

## Story Notes
- Target viewer should feel how Codex is used as a practical learning accelerator.
- Every centering step must be a smooth scroll from the current viewport state (no jumps).
- For #p* targets, spotlight the entire containing white card, not only the heading line.
- Move cursor smoothly to each target before spotlight/highlight.
- Prompt flow is prompt spotlight first, then copy-button click (hold 0.5s), then paired task-box spotlight without extra scrolling.
- Prompt hold time scales with content length using the 2s-per-5-lines rule.
- Checkpoint sections are shown without revealing answers to preserve learner reflection.
- Outcomes emphasis is the P1 block that starts with "By the end of this page you should be able to...".
- Audio includes a subtle elevator-music bed plus click/scroll interaction cues.

## How To Steer Next Iteration
- Compare the current video output with these beats and directives.
- Edit this story or directives file first, then update tool arguments/workflow actions to match.
- Regenerate and repeat until visual pacing and clarity match the intended story.

## Performance Status
- Raw video generation (seconds): `344.532`
- Post processing (seconds): `99.129`
- Transcode step (seconds): `29.342`
- Effects step (seconds): `69.786`
- Video length (seconds): `329.730`
- Video size on disk (bytes): `22762555`
- Has audio stream: `True`
- Directives generation (seconds): `0.001030`
- Story generation (seconds): `0.000566`
- Sidecar generation total (seconds): `0.001597`
